[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
peg = "0.8"
serde = { version = "1.0" }
bincode = "1.3"
quinn = "0.8"
bytes = "1.2"
tokio = "1.21"
